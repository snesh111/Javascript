const date = new Date();
console.log(date);


// we can also pass arguments to set time as we want 
// date(year,month,day,hour,minutes,seconds,ms)

const year = date.getFullYear();
console.log(year);

const month = date.getMonth();
console.log(month);